Using the provided image "Clinic Preview" create a react application
that renders a scrollable list of these cards based on the provided
JSON. 

The list should be in the order from top->down based on the lowest
distance value. 
